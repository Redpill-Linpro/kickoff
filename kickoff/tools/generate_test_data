#!/bin/bash

host="192.168.50.4"

cat ../conf/oui.txt | egrep '\s+[A-Z0-9]{2}-[A-Z0-9]{2}-[A-Z0-9]{2}' | awk '{print $1}' | while read vendor ; do
  mac="${vendor}-00-00-00"
  curl -I http://${host}/bootstrap/mac-${mac}.ipxe 2>&1
done

